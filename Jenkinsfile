pipeline {
    agent any

    stages {
        stage('Clone Check') {
            steps {
                echo 'Pipeline is working'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
